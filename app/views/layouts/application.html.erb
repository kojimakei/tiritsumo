<!DOCTYPE html>
<html>
  <head>
    <title>Tiritsumo</title>
    <%= favicon_link_tag 'mountain_yama.png'%>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>    
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <link href="https://use.fontawesome.com/releases/v5.6.1/css/all.css" rel="stylesheet">
  </head>

  <body>
    <header>  
      <nav class="navbar navbar-light navbar-expand-lg">
          <%= link_to image_tag("1.png", class:"mountain-icon"), "/" %>
        <button
        class="navbar-toggler"
        type="button"
        data-toggle="collapse"
        data-target="#responsiveMenu"
        aria-controls="responsiveMenu"
        aria-expanded="false"
        aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>

          <div class="collapse navbar-collapse" id="responsiveMenu">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item"><%= link_to 'みんなのちりつも', stacks_path, class: "nav-link" %></li>
              <% if user_signed_in? %>
                <li class="nav-item"><%= link_to 'ルームを見る', rooms_path, class: "nav-link" %></li> 
                <li class="nav-item"><%= link_to '積み上げる', new_stack_path, class: "nav-link" %></li> 
                <li class="nav-item"><%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: "nav-link" %></li> 
                <% if current_user.image.attached? %>
                  <li class="nav-item"><%= link_to image_tag(current_user.image.variant(resize_to_fill: [55, 55]), class: 'user-image'), user_path(current_user.id) %></li> 
                <%else%>
                  <li class="nav-item"><%= link_to current_user.nickname, user_path(current_user.id),style: "color:black", class: "nav-link" %></li>
                <%end%>
                <li class="nav-item m">目標: <%= current_user.goal%></li>
                <li class="nav-item m">残り: <%= (current_user.deadline.to_date - Date.today).to_i %>日</li>              
              <% else %>
                <li class="nav-item"><%= link_to 'ログイン', new_user_session_path, class: "nav-link" %></li>
                <li class="nav-item"><%= link_to 'ゲストログイン（閲覧用）', users_guest_sign_in_path, method: :post, class: "nav-link" %></li>
                <li class="nav-item"><%= link_to '新規登録', new_user_registration_path, class: "nav-link" %></li>
              <% end %>
          </div>
          </div>
        </div>
      </nav>
    </header>

    <%= yield %>

    <%# <footer class="fixed-bottom">
      <div class="text-center p-3" style="background-color: rgba(0, 0, 0, 0.2)">
        © 2020 Copyright:Tiritsumo
      </div>
    </footer> %>
  </body>
</html>